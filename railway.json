{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "Dockerfile"
    },
    "deploy": {
        "startCommand": "echo '[DEPLOY] Checking files...' && ls -R dist && echo '[DEPLOY] Pushing DB...' && npx prisma db push --accept-data-loss && echo '[DEPLOY] Seeding...' && npx prisma db seed && echo '[DEPLOY] Starting Node...' && node dist/index.js",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}